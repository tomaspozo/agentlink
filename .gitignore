# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.*

# Node (if any tooling is used)
node_modules/

# Misc
*.log
.claude/
.cursor/

# Locally-installed companion skills land in .agents/ and stay ignored — but the
# Codex repo marketplace lives at .agents/plugins/marketplace.json and MUST ship.
# (Codex looks for the marketplace there, not in .codex-plugin/. Git won't descend
# into an excluded directory, so each level is re-included explicitly.)
.agents/*
!.agents/plugins/
.agents/plugins/*
!.agents/plugins/marketplace.json